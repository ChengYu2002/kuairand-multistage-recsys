# Python 3.11 (arm64)。3.12+ / 3.14 上 torch / lightgbm / faiss 未必有轮子。
#   uv venv --python 3.11 && source .venv/bin/activate
#   uv pip install -r requirements.txt

# --- Week 1: 数据与特征管线 ---
numpy>=2.0
pandas>=2.2            # 注意: 3.0 起 copy-on-write 为默认行为
polars>=1.20
pyarrow>=18.0
duckdb>=1.1
scipy>=1.13
scikit-learn>=1.5
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8

# --- Week 2: 模型 (用到时再装) ---
# torch>=2.4

# --- P1: 粗排与 ANN ---
# lightgbm>=4.3
# faiss-cpu>=1.8

# --- dev ---
pytest>=8.0
ruff>=0.5
jupyterlab>=4.2
